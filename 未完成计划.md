# 未完成计划

本文件完全由 AI 维护，用于记录项目的实施计划与进度。移除已实现部分，仅保留未完成项与备忘。

## 待办

### 单项 bug

- [ ] 时间戳问题 （注意 Aegisub 的帧的时间戳位置为两帧中值，具体参考源码中的算法代码）
  视频每帧的时间戳（时间码）显示逻辑与源码不符
  字幕时间戳与视频时间戳对不上
  保存时间码文件功能的时间码值与源码不一致
  关键帧文件与源码一致（已实现✔）

- [ ] 视频侧栏工具拖动时每个浏览器帧都渲染导致性能问题，这种实时渲染应当有统一的异步弃帧优化（音频栏拖动时也有这个渲染性能问题）（也可能不是渲染导致的，是DOM更新导致的）

- [ ] 音量大小滑条数值虽然存储，但在每次刷新页面时不会载入，导致每次刷新页面都是默认音量

- [ ] 左下角从原生切换到 WebCodecs 后无法正常渲染视频画面

## 备忘

单一功能的备忘写在代码注释中，总体架构备忘写在下方，禁止滥用备忘

- 多语言底座已实现（po 直接引用源码目录，仓库零副本）：vite.config.ts 的 `poLocalesPlugin` dev 时把 `/locales/*` 映射到 `Aegisub/po/*`、build 时 emit 到 `dist/locales/`；运行时核心在 src/ui/i18n.ts（自写 po 解析器 + Plural-Forms 递归下降求值 + 精确/去加速键两级索引 + `t/tPlain/tPlural/tFmt`），LINGUAS 经 `?raw` import 内联；命令 label/help、default_menu.json 的 text/tlcontext 均为 pot msgid 可直接查表；`app/language` 打开 LanguageDialog（dialogs.tsx），选择写入 `App/Language` config（源码同键），默认语言按 navigator.languages 匹配 LINGUAS（精确→Latn 变体→前缀）；语言切换经 `useLocaleVersion` 通知重渲染，aegisubMenus 的菜单结构按语言缓存重建（getAegisubMenus/getGridContext/getVideoContext，原常量已删除）
- 多语言组件层已全面接入：全部 UI 组件约 450 处用户可见字面串包 `tPlain()`（含 PAGES 数据在渲染点包装的策略）；数据串（OPT key、下拉 value、命令 id、逻辑比较值）一律不包。撤销描述（apply 第二参）统一为源码 msgid 小写措辞（"timing"/"sort"/"line insertion"/"style change" 等，逐条对照源码 Commit 调用），可命中 po 翻译并正确参与同标签合并；Undo/Redo 菜单（"Undo %s"）与状态栏显示经 tPlain 二次翻译。web 特有文案（wx stock 按钮词 OK/Apply、状态栏消息、aria-label、web 专属页签）在 src/ui/i18nSupplement.ts 补充词典（zh_CN 185 条，仅 po 未命中时兜底；PlayResX/格式名/IndexedDB 等技术标识刻意不译，未收录语言回退英文）
- scripts/ 下不要放 minified/调试产物（oxlint `.` 会扫出数百错误）
- 缓存三件套已实现于 IndexedDB（主库 v4）：关键帧缓存 `keyframes` store（键 `crc32(文件名)_大小_修改秒`，对应 ffindex 命名；逐出按 `Provider/FFmpegSource/Cache/Files`/`Size`，源码 UI 不暴露这两项，仅 config.json 可改）；自动保存 `projects` store 的 `autosave:<时间戳>` 键（`名称.时间戳.AUTOSAVE.ass` 语义，旧单槽 `autosave` 键只读回退）+ `App/Auto/Backup` 的 `backup:<文件名>` 键；config.json/hotkey.json 在 `config` store，localStorage 为同步回退 + 旧数据一次性迁移，导入导出在 Preferences → Config files 页（导出与桌面版格式互通，热键导入兼容旧版 {modifiers,key} 结构）
- 工具值缓存已对齐源码 OPT_SET 语义：音频三滑块（`Audio/Zoom/Horizontal|Vertical`、`Audio/Volume`，AudioPane 250ms 防抖写回）；Select Lines / Shift Times 弹窗关闭时写回（析构 OPT_SET：OK/Cancel 都保存）；Style Editor 预览文本（`Tool/Style Editor/Preview Text`，DEFAULT_CONFIG 已补齐该子树）
- `native/scripts/build_wasm.ps1`：emsdk 6.x 的 emcmake 是 `.exe` 不是 `.bat`（脚本已兼容两者）；SINGLE_FILE=1 只产出 JS（wasm 内嵌），独立 `.wasm` 复制步骤已改为存在才复制；PS 5.1 下 emsdk.bat 的 benign stderr 会被 `$ErrorActionPreference='Stop'` 包装成 NativeCommandError，需临时降级 Continue
