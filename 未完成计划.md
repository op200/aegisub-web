# 未完成计划

本文件完全由 AI 维护，用于记录项目的实施计划与进度

## 待办

- [ ] 四角定位工具的性能问题依旧存在，模仿另外几个视频侧栏工具是怎么解决性能问题的

## 备忘

单一功能的备忘写在代码注释中，总体架构备忘写在下方，禁止滥用备忘

- 菜单项的命令必须在 `COMMANDS`（commands.ts）里有条目：`MenuBar` 的 label 回退链是 `item.label ?? t(COMMANDS[cmd]?.label ?? cmd)`，缺失时会把原始命令 id（如 `subtitle/open/autosave`）直接显示在菜单里。`default_menu_platform.json` 只给 command 不给 text 的项全部依赖 COMMANDS 提供 label/help（本次补上了 `subtitle/open/charset` 与 `subtitle/open/autosave`）
- 启动不得自动恢复自动保存（对齐源码 frame_main：恢复只能由 File → Open Autosaved Subtitles... 显式触发）：启动静默载入历史自动保存会把 UI 拖进异常状态——旧核心遗留的空文档经 TS 回退运行时的 restore（structuredClone）后网格 0 行、编辑栏「未选中行」、插入行无操作（wasm 核心的 open 会补 1 行，但补出的是空文本行，同样无意义）。`client.restore` 全仓只有两个调用点：`newSubtitles` 与 `restoreAutosave`（显式命令），启动 effect 只走 `client.state()`
- 视觉工具 pointermove 里禁止 setState：四角透视曾每移动一次 `setPersp({...state})` 触发整棵 PreviewPane 重渲染（其余工具只走 `scheduleDragCommit`，零 setState）。正确形态 = 就地改写 `perspRef.current`（同一对象引用）+ `scheduleOverlayRender()`（rAF 合流重绘）+ `scheduleDragCommit` 提交；实测（200/800 行文档、dummy 视频、含网格）透视与标准拖放工具帧间隔完全一致（avg 16.67 / p95 16.8 / 无 >20ms 帧）
- 四角透视会话 effect 的依赖表必须含 `windowZoom`：`PerspState` 里的四角/网格坐标是「屏幕像素」（`perspTextToPersp` 经 `mediaRect()` 换算），媒体框几何一变就必须重跑 DoRefresh；平移/内容缩放/**窗口缩放**三者都改变媒体框。`renderOverlay` 自身不需要 `windowZoom`（stage 尺寸随之变化，ResizeObserver 会触发重绘）

- 后台索引 worker 架构（media/peaks.worker.ts）：音频峰值/频谱分析与视频关键帧扫描全部在 worker 线程执行（FFMS2 索引线程语义）；主线程侧入口 audioPeaks.ts（extractAudioPeaks 路由 + delta 装配）与 demux.ts（extractVideoKeyframes 委托），二者对 worker 文件仅 `new URL(...)` 引用与 type import，无运行时循环依赖。协议：meta（主线程按 buckets/columns 一次性分配 live buffer）→ progress（桶/brick 增量切片，worker 侧必须按 meta 容量钳制防 RangeError）→ done（transfer 终值）/error/scan-done。worker 线程可用 WebCodecs；web-demuxer 需 workerWindow.ts 先注入 `window=self`（否则其内部 data-URL worker 的 wasm fetch 被 CORS 拦截）
- 多语言底座已实现（po 直接引用源码目录，仓库零副本）：vite.config.ts 的 `poLocalesPlugin` dev 时把 `/locales/*` 映射到 `Aegisub/po/*`、build 时 emit 到 `dist/locales/`；运行时核心在 src/ui/i18n.ts（自写 po 解析器 + Plural-Forms 递归下降求值 + 精确/去加速键两级索引 + `t/tPlain/tPlural/tFmt`），LINGUAS 经 `?raw` import 内联；命令 label/help、default_menu.json 的 text/tlcontext 均为 pot msgid 可直接查表；`app/language` 打开 LanguageDialog（dialogs.tsx），选择写入 `App/Language` config（源码同键），默认语言按 navigator.languages 匹配 LINGUAS（精确→Latn 变体→前缀）；语言切换经 `useLocaleVersion` 通知重渲染，aegisubMenus 的菜单结构按语言缓存重建（getAegisubMenus/getGridContext/getVideoContext，原常量已删除）
- 多语言组件层已全面接入：全部 UI 组件约 450 处用户可见字面串包 `tPlain()`（含 PAGES 数据在渲染点包装的策略）；数据串（OPT key、下拉 value、命令 id、逻辑比较值）一律不包。撤销描述（apply 第二参）统一为源码 msgid 小写措辞（"timing"/"sort"/"line insertion"/"style change" 等，逐条对照源码 Commit 调用），可命中 po 翻译并正确参与同标签合并；Undo/Redo 菜单（"Undo %s"）与状态栏显示经 tPlain 二次翻译。web 特有文案（wx stock 按钮词 OK/Apply、状态栏消息、aria-label、web 专属页签）在 src/ui/i18nSupplement.ts 补充词典（zh_CN 185 条，仅 po 未命中时兜底；PlayResX/格式名/IndexedDB 等技术标识刻意不译，未收录语言回退英文）
- scripts/ 下不要放 minified/调试产物（oxlint `.` 会扫出数百错误）
- 缓存三件套已实现于 IndexedDB（主库 v4）：关键帧缓存 `keyframes` store（键 `crc32(文件名)_大小_修改秒`，对应 ffindex 命名；逐出按 `Provider/FFmpegSource/Cache/Files`/`Size`，源码 UI 不暴露这两项，仅 config.json 可改）；自动保存 `projects` store 的 `autosave:<时间戳>` 键（`名称.时间戳.AUTOSAVE.ass` 语义，旧单槽 `autosave` 键只读回退）+ `App/Auto/Backup` 的 `backup:<文件名>` 键；config.json/hotkey.json 在 `config` store，localStorage 为同步回退 + 旧数据一次性迁移，导入导出在 Preferences → Config files 页（导出与桌面版格式互通，热键导入兼容旧版 {modifiers,key} 结构）
- 工具值缓存已对齐源码 OPT_SET 语义：音频三滑块（`Audio/Zoom/Horizontal|Vertical`、`Audio/Volume`，AudioPane 250ms 防抖写回）；Select Lines / Shift Times 弹窗关闭时写回（析构 OPT_SET：OK/Cancel 都保存）；Style Editor 预览文本（`Tool/Style Editor/Preview Text`，DEFAULT_CONFIG 已补齐该子树）
- `native/scripts/build_wasm.ps1`：emsdk 6.x 的 emcmake 是 `.exe` 不是 `.bat`（脚本已兼容两者）；SINGLE_FILE=1 只产出 JS（wasm 内嵌），独立 `.wasm` 复制步骤已改为存在才复制；PS 5.1 下 emsdk.bat 的 benign stderr 会被 `$ErrorActionPreference='Stop'` 包装成 NativeCommandError，需临时降级 Continue
- 时间码首帧偏移语义（2026-09 调研定论）：谱系 = 不归一化是 Aegisub 原始行为 → 归一化由 2010-09 SVN r4791（1dedfb18，Thomas Goyne）引入（理由「当年音频链路不支持非零起始」）→ arch1t3cht b7d228c0ce（2024-11-03）revert 回不归一（保留容器起始延迟，修复 TS#21/#169）。截至 2026-09：fork feature（tip 9bfd500）不归一、migration05 与 TS master（vendored 来源）均仍归一化，「保留偏移」未进主线——本项目跟随主线：载入即归一化（首帧记 0）。社区两难未决：TS#169/#21（真实 mkv 首帧 23ms，mpv/mpc-hc 不归一 → Aegisub 差 1 帧）vs arch1t3cht#172（不归一在 .m2ts 蓝光上回归，首帧 1:10:00.000 容器伪影）；理想方案需 FFmpeg start_time（source filter 未暴露）或 mpv rebase-start-time 式可配置。播放器普遍做法：呈现层全流统一 rebase + 调度层用容器绝对时间戳（流间关系不破坏），与编辑器单流重定义本质不同。数学要点：numerator/last 在归一与否下数值相同（back-front 差值不变），故「保留偏移」导出 = 表内写原始 PTS 值、表尾外推复用归一化公式（fork Framerate::Save 同形）。web 版偏移感知 UI：载入视频时偏移 > (1000/fps)/5（每帧时长 1/5）弹提醒；导出时间码时表首帧非 0 弹「保留偏移/归一化到 0」选择（vfr.ts serializeTimecodesKeepOffset + App.tsx）
- 媒体跳转帧对齐（2026-09 修复，PreviewPane）：原生 `<video>` 元素时间轴以容器原始 PTS 为基准（首帧 PTS ≠ 0 时整体偏移，07.va.mp4 = 7ms），应用时间轴按 FFMS2 normalize_timecodes 归一化到首帧 = 0 → 不换算时 `dialog_jumpto.cpp` 的 FrameAtTime(a) 与元素"包含 a 的原始时间"帧恒差 1 帧（偏移大则差 2+），即用户报的 n±1~2 帧。修法：onLoadedMetadata 内注册 rVFC 取首帧 mediaTime 为偏移（首帧 present 即回调，presentedFrames=1，无需 seek；元素重挂载/切通道时清零）；seek 目标 = 帧表时间 + 偏移 + 1ms，元素位置→应用时间 = ptsToMs(t) − ptsToMs(偏移)。两个精度坑：偏移必须保留浮点秒（截断成整数 ms 会差 <1ms 落到前一帧）；帧表本身截断原始 PTS，故 seek 需前移 1ms 补偿（帧间隔 ≫ 1ms 不会越帧）。元素→应用必须"分别截断再相减"（先减后截会与帧表口径差 1ms，使 currentFrame 退帧）；元素时长同理换算。WebCodecs 通道（mkv）用同一 demuxer + baseMs，本就自洽无需换算。验证法：IndexedDB 'keyframes' 缓存的 frameTimesMs 就是应用真实帧表（须等扫描落库再读，否则退化为 CFR 回退表会误判），对拍"rVFC mediaTime 所属帧 == 表上 FrameAtTime(a) == UI `.video-position` 帧号"；offset.mp4（首帧 6.999ms）与 07.va.mp4（34571 帧 VFR）四点全 match
- 字幕栏 tooltip：源码 base_grid.cpp 无任何 SetToolTip，网格文本单元格与表头的 hover 提示已移除（这两处为 web 侧额外添加）；列 Description 在源码只用于表头右键「列显隐」菜单项文案（base_grid.cpp:562），web 侧同样用 GRID_COLUMNS[].description 作菜单标签
- 视频面板高度语义（video_box.cpp 挂靠模式）：topSizer 行高 = max(视频框, 侧栏自然高)，其中侧栏 = visualToolBar(proportion 1) + visualSubToolBar(proportion 0)；视频缩放过小时 wx 取侧栏高撑高视频框（不压缩工具行）。web 侧实现 = App 的 workspace 首行 `minmax(panelHeight px, min-content)` + `.preview-panel`/`.video-content` 首行 `minmax(min-content, 1fr)`（`.visual-toolbar` 的 min-content 贡献即工具列自然高，stage 跨两行故按 max(侧栏, 视频) 分配；面板 min-content = 侧栏 + 56）。移动端（≤900px，面板固定高）已在 media query 里恢复可压缩行
- 原生 select 垂直居中：Blink 只在内容盒能容纳文字行盒（16px 字体约 21px）时才居中，否则贴顶溢出（观感偏下）。`line-height` 对 select 内部文本无效，唯一杠杆是 `font-size` 与上下 `padding`（本项目的 `.edit-panel select` 去掉上下 padding 后内容盒 22px 容纳行盒即对齐）
- 视觉工具弃帧通道：所有视觉工具（含四角透视）的拖拽提交必须走 PreviewPane 的 `scheduleDragCommit`（= rafThrottle 的 serialLatest，在途不排队、只保留最新目标）；直接同步调 `*CommitValues` 会逐个排队，一次提交含全文档序列化 + libass 渲染，pointermove 数百 Hz 时鼠标停住后画面仍在回放中间位置。验证法：同一任务内 `dispatchEvent(new PointerEvent('pointermove'))` 爆发（`page.mouse.move` 连发会被 Chromium 合并），包 `Worker.prototype.postMessage` 数 `method==='apply'` 次数
- WASM 核心的 JSON 取值必须用 ConstVisitor 判型（`native/src/aegisub_core_api.cpp` 的 JsonProbe/json_str/json_int/json_double/json_bool）：cajun 的隐式转换 `CastTo<T>` 在类型不匹配时 `throw Exception("Bad cast")`，而 wasm 构建未启用 C++ 异常 → throw 直接 abort 整个模块（状态栏显示 `Aborted(undefined)`，try/catch 全失效且模块此后不可用）。JS 数字不区分整/浮点（`JSON.stringify(48)`→Integer、`48.5`→Double），所以整数值字段用 json_double 或浮点值字段用 json_int 都会炸；症状是"某次样式保存后所有命令静默失效"，排查用 node 直接加载 wasm 产物逐字段试（探针走 worker 协议最省事）
- WASM 核心的样式 id 即样式名（`style_to_json` 里 `o["id"] = s.name`，`find_style` 按名字查；cue id 才是 `std::to_string(d.Id)`）→ 改名会改变样式句柄，而源码侧 UI 持 `AssStyle*` 指针（`dialog_style_editor.cpp` 重名检查就是 `existing != style` 指针比较）。UI 层对策：句柄记"打开时的 id + 最近应用的名字"，解析先按 id 再按名字回退，改名落地后再把句柄切到新 id（App.tsx 的 editingStyleHandle / StyleManagerDialog 的 editorCurrent）——否则第二次改名时 id 与旧名双双失配，对话框会被卸载、重名检查会误判自身
- 样式编辑器的语义对齐（dialog_style_editor.cpp）：OK=`Apply(true,true)`、Apply=`Apply(true,false)`、Cancel=`Apply(false,true)`；重名报错不关闭；改名仅当前脚本的非新建样式且 `StyleRenamer::NeedsReplace()` 为真时询问 YES/NO/CANCEL（Cancel 中止不关闭、No 只改样式名不动脚本引用、Yes 连同 `\r` 标签与行 Style 一并改，单次 Commit 一步撤销）；预览文本析构写回 `Tool/Style Editor/Preview Text`。`ass_override.h` 无 include guard（由 ass_dialogue.h 引入），native 侧不可重复 include
